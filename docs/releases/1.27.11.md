# Polaris 1.27.11

- 修复多课表场景下桌面小组件刷新闹钟只跟随激活课表的问题：现在按所有已绑定课表汇总最近的上下课边界。
- 升级备份格式至 V2：备份并恢复每个课表的学习计划、考试/DDL等事件和设置；恢复前完整校验并通过单次提交清理旧数据，提交失败时反向恢复操作前的本机状态，并区分内存恢复与磁盘持久化确认，V1 备份仍可读取。背景图和头像继续仅保存图片 URI。
- 同步 README 与隐私政策：明确自动版本检查默认开启、每天最多一次，APK 仅在用户主动下载时获取，课程数据不上传。
- 固化三校匿名 PDF 回归样例：西邮、西理工、杭电分别走对应解析模型，独立校验课程名、节次、周次、地点和教师，缺失样例不再静默跳过。
- 修正西邮课表左侧纯节次行标题被通用解析器误判为课程的问题，并将该版式纳入合成解析回归。
- 提高标签发布前回归门槛：标签 push 改跑完整仪器测试，结合全量单测覆盖导入、编辑、旋转和存储路径。
- 清理应用自有 Lint 告警：复用自绘路径、Toast 改用弱引用并补齐水平滑动控件的 performClick；备份恢复执行与统计从 MainActivity 抽为独立控制器并补充回归测试。
