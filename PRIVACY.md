# 隐私政策

本政策适用于 Polaris课程表（下称「本应用」）。本应用是开源软件（GPL-3.0），源码见项目仓库。

## 一句话总结

**Polaris 默认开启每天最多一次的自动版本检查，仅访问官方更新地址读取公开版本清单；只有你主动选择下载更新时才会获取安装包。课程、课表、学校、账户资料、设置及导入文件不会上传。**

## 数据存储

| 数据类型 | 存储位置 | 说明 |
|---|---|---|
| 课程数据、课表配置、多课表、账号资料（昵称/头像） | 设备本地 `SharedPreferences` | 卸载应用或清除数据后丢失 |
| 背景图/头像图片 | 设备本地（系统相册 URI 引用） | 应用不复制图片内容 |
| 提醒设置 | 设备本地 | 由系统闹钟/通知服务调度 |
| 更新检查状态 | 设备本地 `SharedPreferences`（`polaris_update`） | 仅记录自动检查开关、最近成功检查时间、忽略的版本号与待安装文件路径，不含任何个人信息 |

## 数据导出（由你主动控制）

- **分享/备份**：导出 `.polaris`（课表文件）、`.polarisbackup`（课表、计划、考试/DDL等事件与设置）、`.ics`（日历）、`.csv` 等文件由你主动触发，文件内容由你自行保管与分发；备份中的背景图和头像仅保存图片 URI，不包含图片内容
- **PDF 导入**：导入的 PDF 仅在本机内存中解析，解析后不保留副本

## 外部 AI 识别

「AI 识别导入」功能中，课表图片与识别指令通过系统分享面板发送给你**主动选择的**外部 AI 应用。发送行为由你确认，Polaris 不代收、不转发、不保存识别内容（识别结果文本由你手动粘贴回应用，仅存本机）。

## 应用内更新（唯一联网场景）

- **何时联网**：默认开启的自动版本检查每天最多一次；也可在「更多 → 更新 → 检查更新」手动检查。自动检查只读取公开版本信息，发现更新不会自动下载或安装；只有你主动点击下载后才会获取 APK
- **访问什么**：Polaris 官方 GitHub / Gitee Release 中的公开版本信息、`latest.json` 与正式签名安装包（域名白名单固定写死在应用内）
- **携带什么**：请求仅包含访问更新所需的普通 HTTPS 请求头（含应用版本与 Android 版本 User-Agent），不主动提交课表、学号、学校、账户名称、设备标识或统计埋点
- **如何安装**：下载包经大小、SHA-256、包名/版本与签名四层校验后，交给 Android 系统安装器，由你在系统界面确认覆盖安装；应用不尝试静默安装

## 权限说明

| 权限 | 用途 |
|---|---|
| 通知（POST_NOTIFICATIONS） | 课程课前提醒 |
| 精确闹钟（SCHEDULE_EXACT_ALARM） | 提醒按时触发 |
| 悬浮窗（SYSTEM_ALERT_WINDOW） | 提醒弹窗显示 |
| 网络（INTERNET） | 自动或手动版本检查，以及用户主动开始的官方 APK 下载 |
| 请求安装应用（REQUEST_INSTALL_PACKAGES） | 将已验证的更新 APK 交给 Android 系统安装器，由用户确认安装 |

除更新所需的两个权限外，其余权限均可拒绝，拒绝后仅对应功能不可用，不影响其他功能。关闭自动检查后，应用不会在启动时自动发起版本检查；手动检查仍可用。未选择下载或安装时，不会获取 APK，也不会调用系统安装器。

## 联系方式

隐私相关问题请通过项目仓库 Issue 或 README 中列出的联系邮箱反馈。
